/**
 * 这个文件作为组件的目录
 * 目的是统一管理对外输出的组件，方便分类
 */

/**
 * 布局组件
 */
import Footer from './Footer';
import { Question, SelectLang } from './Header';
import { AvatarDropdown, AvatarName } from './Header/AvatarDropdown';

export { Footer, Question, SelectLang, AvatarDropdown, AvatarName };
